[package]
name = "sensor"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sensor"
path = "src/main.rs"

[dependencies]
regex = "1"
serde_json = "1"
uuid = { version = "1", features = ["v4"] }
chrono = { version = "0.4", features = ["clock"] }
